<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# 開発標準ルール（常時自動適用・GEMINI & AGENTS共通）

## 0. 完全自律実行規範（Turbo Mode / Autonomous Operation）
- **質問ツール・承認モーダルの停止禁止**:
  ターボモードおよび自律実行設定下において、`ask_question` による不要な質問ダイアログの表示や、`RequestFeedback: true` による作業中断・承認待ちモーダルの表示を禁止する。
- **完遂の義務**:
  ユーザーの手を煩わせることなく、指示された要件に対して設計・実装・コード検証・ビルド確認・本番デプロイまでを完全に自律して実行し、1ターンのうちに完遂すること。
- **ユーザー選択肢の強要排除**:
  ユーザーが選択肢を選んでSubmitするだけの不要なやり取りを一切発生させず、AI側で最適な判断を行って作業を進めること。

## 1. 既存機能・ページ・設定の絶対不可侵・退縮（リグレッション）禁止規範（最重要）
- **指示なき仕様変更・リファクタリング・簡略化の全面禁止**:
  ユーザーからの明示的な指示がない限り、過去に実装した機能、UIコンポーネント、レイアウト構造、データモデル、設定項目を「良かれと思って」変更・オミット・非表示化・削除・簡略化することを固く禁ずる。
- **永久保持すべき機能群の明記**:
  - **コラム下書き（Draft）機能**: 保存・下書き一覧表示・編集・公開/下書きトグル機能
  - **モーダルヘッダー＆タイトル構成**: コラム詳細およびコンテンツ詳細を開いた際の「登録ヘッダー画像バナー」と「特大タイトル（h1）」レイアウト
  - **アクションボタングループの統一**: 詳細モーダル内の保存・編集・削除・読了などのボタン構成および操作性
  - **AIコンテンツ生成機能**: 概要・メリット生成、ブロック別AI画像生成、読了目安時間・文字数の自動計算機能
  - **一覧ページの共通検索・フィルター**:
    サロン内の一覧ページ（`/contents`, `/columns` 等）において、「こだわり条件検索（折りたたみパネル）」、「HARMSジャンルフィルター（すべて表示, [H], [A], [R], [M], [S]）」、「運用者（投稿者）検索ドロップダウン」、「こだわり条件タグチェックボックス」、「該当件数表示＆条件リセットボタン」の全セットを必須とし、勝手に間引いたり簡易表示へ置き換えることを固く禁止する。
  - **独自ツール・診断機能**: HARMS診断ツール、外部ツールビューアー、各種連携ツールの完全保持。
- **部分編集（Surgical Edits）の徹底**:
  ファイル全体の全置換による既存コードの意図せぬ脱落事故を防ぐため、原則として局所的な差分置換（`replace_file_content`）を用いること。
- **事前インベントリ検査と事後突き合わせ照合**:
  コード編集を行う際は、変更前のUI要素・ボタン・フォーム・データモデルを事前に検査し、変更後も漏れなく全て存在していることをコード上で照合確認すること。1点でも欠落や退縮がある場合はデプロイを行ってはならない。

## 2. テキスト・デザイン規範
- **Unicode絵文字（unicode emojis）の完全使用禁止**:
  テキスト、UI、ログ、チャット応答、コード内コメントのすべてで絵文字を完全に排除し、アイコンは Lucide アイコン等のSVGコンポーネントのみを使用する。
- **横スクロール（スライドバー）の発生禁止**:
  タブやフィルター、カード群は `flex-wrap: wrap` またはレスポンシブ配置で自然に折り返すこと。
- **条件検索パネルの配置**:
  折りたたみ式とし、開閉ボタンは右端（`width: 100%`, `marginLeft: auto`）に整列配置する。
- **タグ/ボタンの視認性**:
  未選択時＝対応色の淡い背景・透明度0.6、選択時＝ソリッドテーマ色＋白文字＋シャドウで明快に切り替える。
- **ユーザー素材の尊重**:
  ユーザーが用意した独自ビジュアル素材（画像・図解等）がある場合、強制的なシステム装飾枠を被せず素材そのままの表示を優先する。

## 3. マルチデバイス（PC・モバイル）同等性＆データ保護規範
- **PC・モバイル100%同等性**:
  PCブラウザ版とモバイル版で、表示コンテンツ・投稿・機能の100%同等性を維持する。片方のみの機能追加や表示不一致を許容しない。
- **データ消失・初期値上書きの防止**:
  ユーザーデータ・設定・投稿内容・並び順が開発やアップデートによって消失・破損・非表示化されない構造を保証する。
- **単一情報源（Single Source of Truth）**:
  データベースのマスタードキュメント（`portal_data/`）を単一の絶対基準とし、削除データが初期デフォルト値補完ロジック等で勝手に復活・表示されないよう削除判定を厳格化する。

## 4. UX・データ分析規範
- **構造化フィードバック**:
  ユーザーからの理由入力やフィードバック収集時（退会理由やアンケート等）は、選択肢ラジオボタン等でデータ構造を標準化し、管理画面で定量計測・集計できるダッシュボードをセットで構築する。
- **独自診断ツールの維持**:
  HARMS診断ツール等の独自に作成したツールや診断機能は、アップデートの過程で排除せず保持・更新する。
